import "./App.css";
import Homepage from "./components/Homepage.jsx";

// app only needs to load homepage because homepage holds the rest of the page
function App() {
  return <Homepage />;
}

export default App;
